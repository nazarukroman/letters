{
  "name": "letters",
  "version": "1.0.0",
  "description": "5letters helpers app",
  "author": "Nazaruk Roman",
  "license": "ISC",
  "scripts": {
    "dev": "pnpm -F client run dev",
    "dev:server": "pnpm -F letters-server run dev",
    "build": "pnpm -F client run build",
    "seed": "pnpm -F letters-server run seed",
    "format": "oxfmt",
    "format:check": "oxfmt --check",
    "lint": "oxlint",
    "prepare": "husky"
  },
  "packageManager": "pnpm@10.33.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "devDependencies": {
    "husky": "9.1.7",
    "lint-staged": "17.3.0",
    "oxfmt": "0.65.0",
    "oxlint": "1.80.0"
  }
}
